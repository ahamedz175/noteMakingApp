<DOCTYPE html>

<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"> 
	<title>Node Tutorial</title>
</head>
 
<body>
     <div class="container">
        <div class="d-flex justify-content-center py-3">
	<ul class="nav nav-pills">
	<!-- <li class="nav-item">
	   <a href="/" class="nav-link active" aria--current="page">Node Tutorial</a>
	</li> -->
	<% if(currentUser == null) { %>
	<li class="nav-item">
           <a href="/home" class="nav-link">Home</a>
        </li>
	<%}%>
	<% if(currentUser != null) { %>
            <li class="nav-item">
               <a href="/posts" class="nav-link">Notes</a>
            </li>
	    <li class="nav-item">
              <a class="nav-link disabled"> Welcome <%= currentUser.username %>!</a>
            </li>
        <%}%>
	


	<li class="nav-item">
           <a href="/about" class="nav-link">Contact</a>
        </li>

        <% if(currentUser != null) { %>
	    <li class="nav-item">
               <a href="/logout" class="nav-link">Logout</a>
            </li>
        <%}%>
        <% if(currentUser == null) { %> 
            <li class="nav-item">
               <a href="/login" class="nav-link">Log In</a>
            </li>
	    <li class="nav-item">
               <a href="/signup" class="nav-link">Sign Up</a>
            </li>
        <% } %>
        </ul>
 </div>
</div>
 
<div class="container">
  
<%  if(error && error.length > 0) { %>
    <div class="alert alert-danger" role="alert">
      <%= error %>
    </div>
<% } %>

<%  if(info && info.length > 0) { %>
    <div class="alert alert-info" role="alert">
      <%= info %>
    </div>
<% } %>



